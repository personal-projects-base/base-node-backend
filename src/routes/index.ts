import { Router } from 'express';

export const customRoutes = Router();

// Registre aqui somente endpoints próprios da aplicação.
// O CRUD configurado em .gonthera é registrado automaticamente em src/app.ts.
